# docker-compose

services:
  flask:
    build:
      context: ./api
      dockerfile: Dockerfile.flask
    expose:
      - "5000"
    env_file:
      - ./api/.env

  nginx:
    build:
      context: ./frontend
      dockerfile: Dockerfile.nginx
    ports:
      - "80:80"
      - "443:443" 
    volumes:
      - /etc/letsencrypt:/etc/nginx/certs:ro
    depends_on:
      - flask